# Agent Notify MCP

See root README. Env: `AGENT_NOTIFY_SITE` and `AGENT_API_TOKEN` (site-wide solo token or a per-account token). Tools: `agent_notify`. Optional `topic` argument for account-scoped channels.
